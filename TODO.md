# Mora 功能清单

> 最近核对：2026-07-27  
> 本清单以当前源码、测试和 `README.md` 为准；完成状态表示仓库中已有可调用的实现，不代表所有后续增强均已完成。

## 已实现

### 编辑体验

- [x] Toast UI Editor 所见即所得编辑
- [x] Markdown 源码、仅预览和垂直双栏模式
- [x] 标题与标签编辑
- [x] 文档目录与标题跳转
- [x] 当前文档查找、替换和全部替换
- [x] 标题、粗体、斜体、删除线、引用、列表、任务列表、代码、链接、表格等常用 Markdown 命令
- [x] 撤销、重做、剪切、复制、粘贴、全选和常用快捷键
- [x] 浅色、深色、跟随系统主题
- [x] 字体、字号、行高和阅读宽度偏好

### 笔记与文件

- [x] 新建、打开、保存和另存为 `.mdx` 笔记包
- [x] 编辑标题、正文和元数据
- [x] 导入 Markdown 文件
- [x] 最近打开文件列表
- [x] 本地笔记库与标题、正文全文搜索
- [x] 校验 `manifest.json` 格式、版本和加密状态
- [x] 校验 ZIP 路径、重复条目、条目数量和解压体积
- [x] 保持 v1 笔记包兼容

### 图片、附件与资源

- [x] 粘贴图片并写入 `assets/`
- [x] 选择或拖放图片和普通附件
- [x] 附件写入 `attachments/`
- [x] 正文持久化时使用包内相对路径
- [x] 编辑期间通过临时 Blob URL 显示包内资源
- [x] 覆盖保存时保留已有资源目录和兼容目录

### 数据可靠性

- [x] 新建、打开和关闭前的“保存 / 放弃 / 取消”保护
- [x] 原生窗口关闭权限与关闭流程
- [x] 自动保存恢复草稿，包括尚未写入笔记包的新资源
- [x] `.tmp + .bak` 安全保存和失败恢复
- [x] 覆盖保存时创建历史快照
- [x] 历史版本列表、读取和恢复，最多保留 20 份

### 导出

- [x] 导出 Markdown 正文和同名资源目录
- [x] 导出时重写资源相对路径
- [x] 通过系统打印对话框导出 PDF

### 桌面体验与质量

- [x] 紧凑菜单栏、状态栏和当前文件状态提示
- [x] 响应式窗口、键盘焦点和减少动态效果适配
- [x] 前端单元测试与 Rust 集成测试
- [x] 前端构建、Lint、格式检查、Rust 检查和 Tauri 打包流程

## 部分实现或当前限制

- [ ] 全文搜索目前只覆盖在 Mora 中打开或保存过的笔记，尚不会扫描任意磁盘目录。
- [ ] 图片和附件已有大小限制与安全校验，但仍使用内存缓冲和 Base64 传输，不适合超大附件。
- [ ] PDF 导出依赖系统打印能力，尚未内置独立 PDF 渲染器。
- [ ] `thumbnails/` 和 `exports/` 目录会被创建或保留，但尚无封面/缩略图生成与可视化管理功能。
- [ ] 当前以 Windows 桌面版本为主，Linux 和移动端尚未完成适配。

## 未实现 / 待办

### 编辑与渲染

- [ ] Mermaid 图表渲染
    - 当前可保存 `mermaid` 围栏代码块，但只按普通代码块显示。
    - 需要接入 Mermaid 运行库和 Toast UI 自定义渲染器。
    - 需要覆盖源码、预览、所见即所得、打印/PDF、主题切换和安全清理测试。

### 数据与平台

- [ ] AES-256-GCM 笔记加密
- [ ] 云同步
- [ ] 多人协作
- [ ] 任意文件夹扫描、批量建立索引与文件变更监听
- [ ] 大附件流式或分块读写，减少内存和 IPC 开销
- [ ] 封面与缩略图生成、选择和管理
- [ ] Linux 桌面适配
- [ ] 移动端适配

## 约束

- `.mdx` 始终表示 Mora 的 MDXNote ZIP 笔记包，不引入 Web MDX / JSX 语义。
- 新功能不得破坏现有 `.mdx` v1 文件兼容性。
- 文件保存继续遵循 `.tmp + .bak` 安全保存流程。
- 功能完成后应同步更新本文件、相关测试和 `README.md`。
