export type PathIdentity = {
    path: string;
    identity: string;
    available: boolean;
};

export type RecentFileEntry = {
    path: string;
    title: string;
    lastOpenedAt: string;
    available: boolean;
};
